require('zen-mode').setup({
    window = {
        width = 0.80,
        height = 0.90,

        options = {
            signcolumn = "no",
            number = false,
            relativenumber = false,
        },
    }
})
